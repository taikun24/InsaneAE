org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true

# Parchment はパラメータ名と javadoc だけを補うマッピング。
# フィールド名・メソッド名は Mojang 公式のままなので、Mixin の @Shadow / @Invoker には影響しない。
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# The Minecraft version must agree with the NeoForge version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
minecraft_version_range=[1.21.1, 1.22)
# The NeoForge version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.247
# The NeoForge version range can use any version of NeoForge as bounds
neo_version_range=[21.1.0,)
# The loader version range can only use the major version of FML as bounds
loader_version_range=[4,)

## 依存 Mod (CurseMaven から解決する)
# 値は CurseForge の「ファイル ID」。バージョンを上げるときは
# https://www.curseforge.com/minecraft/mc-mods/<mod>/files で対象ファイルを開き、
# URL 末尾の数字をここに入れてコメントのバージョンも直すこと。

# Applied Energistics 2 19.2.17 — 1.21.1 は NeoForge 版のみ。
ae2_file=7027323
# GuideME 21.1.17 — AE2 19.2 が必須依存として要求する (1.20.1 では AE2 に同梱されていた)。
guideme_file=8420861
# MEGA Cells 4.11.0 — 1M〜256M のクラフトストレージを提供する必須依存。
megacells_file=7952621
# Applied Mekanistics 1.6.3 (+ Mekanism 10.7.19.85) — 化学物質セル用の任意依存。
appmek_file=7096962
mekanism_file=7904058
# Mekanism Generators 10.7.19.85 — dev 実行専用。MEGA Cells のレシピが要求するだけで
# この Mod からは参照しない。Mekanism 本体とバージョンを揃えること。
mekanism_generators_file=7904061
# JEI 19.21.2.313 — dev 実行での確認用。
jei_file=8546245

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=insaneae
# The human-readable display name for the mod.
mod_name=InsaneAE
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=LGPL-3.0
# The mod version. See https://semver.org/
mod_version=1.0.7
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
mod_group_id=jp.main.taikun
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=taikun
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=More Storage, Better Gameplay...?
