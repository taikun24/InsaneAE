# 変更履歴

このファイルは `main` (1.20.1) と `1.21.1` の両ブランチに同じ内容で置いてあります。
リリースワークフローが**先頭のバージョン節をそのまま CurseForge の changelog として使う**ので、
見出しの形式 (`## <バージョン>`) は変えないでください。

## 1.0.5

**(1.20.1) AE2 の要求バージョンを 15.4.10 以上に上げました。** 開発・テストを
実際のプレイ環境と同じビルドで行うためです (これまでは 15.2.16 でビルドしていました)。

### 追加

- **超特大 ME インターフェース / 特大パターンプロバイダーのケーブル版 (プレート)** を
  追加しました。AE2 の ME インターフェイス・パターンプロバイダにブロック版とケーブル版が
  あるのと同じで、ケーブルに直接貼り付けられます。枠数も 1 枠あたりの上限も
  ブロック版と同じ (インターフェイス 81 枠 × 21 億 4748 万、パターンプロバイダ 1620 枠)
  で、画面もブロック版と同じものが開きます。作業台でブロック版と 1:1 で行き来できます
  (中身は移らないので、空の状態で持ち替えてください)。
  ブロック版との違いは 2 点だけです。
  - インターフェイスの**溢れたぶんの ME 直送が無い**ので、Mekanism 系の long 搬入を
    受けるならブロック版を使ってください。壊したときの在庫の ME への返却もありません。
  - パターンプロバイダの**押し出し先が貼り付けた面の隣だけ**になります
    (ブロック版は 6 方向すべて)。
- **タスク統合カード** を追加しました。Quantum CPU に挿すと (1 枚、アップグレード枠は
  8 つに拡張)、まとめクラフト 1 回 (1 パターン × N 回) がクラフト CPU の並列処理予算を
  **1 操作しか消費しなくなります**。回数の上限は Quantum CPU 自身の速度
  (加速カード枚数) が受け持つため、超大型ジョブでも同時クラフト数が並列ユニットの
  スレッド数で頭打ちになったり、3 tick 周期でぶれたりしなくなります。
  機械へ押し出す加工パターンの扱いは変わりません (従来どおり 1 押し出し = 1 操作)。
- **(1.20.1) Astral Mekanism & Energistics の自動搬出を ME インターフェイスへ流し込めるようにしました。**
  Astral の機械は 1 回の処理で 922京級を作りますが、Mekanism の自動搬出はアイテムなら
  1 スタック、液体・化学物質なら 1 tick あたり 1024 (既定値) ずつしか出せないため、
  搬出が処理速度にまったく追いつきませんでした。搬出先が ME インターフェイスのときは
  <b>刻まずに中身を全部ネットワークへ渡します</b>。設定枠を入れてあるインターフェイス
  (在庫確保用) は従来どおりで、化学物質は Applied Mekanistics がある場合のみです。
  `config/insaneae-common.toml` の `astralNetworkEject` で切れます。
- **超強化クリエイティブセル** を追加しました。設定した中身を無限に供給するのは
  強化クリエイティブセルと同じですが、**AE2 Crafting Optimizer へは正確な BigInteger 量**
  を報告します。量は ACO が扱える桁 (`bigIntegerMaximumBits`、既定 54,427 bit) の半分で、
  10 進 8 千桁ほど。922京を超える材料を要求する BigInteger クラフトが
  「素材不足」にならなくなります。AE2 本体から見える量は 922京のままです
  (在庫の数え方が 64bit なので、そこは上げようがありません)。
  ACO が無ければ強化クリエイティブセルと差はありません。
  専用のテクスチャは持たず、強化クリエイティブセルの絵を借りています。
- **BigInteger クラフト CPU (実験用)** を追加しました (PR #7 / #8、@syarukasu さん)。
  AE2 標準のクラフト CPU 構造へ組み込む理論上限容量のクラフトストレージで、
  ACO 導入時は公開 API の上限を正確な容量として使います。独自 GUI・ticker・
  Quantum 加速カード・サバイバルレシピは持たず、未割当と分かるよう意図的に
  missing texture (紫黒) を表示する実験用ブロックです。

### 変更

- **Quantum CPU が ACO の正確な BigInteger 実行を直接引き受けるようにしました。**
  ACO が公開している連携境界 (`CraftingTableBatchTarget`) に乗り、
  <b>本物の組み立てを 1 回だけ回して係数を掛ける</b>形で BigInteger 級の注文を処理します。
  要求回数はループ回数を増やしません。正確な会計は ACO が持つため、
  AE2 の long の帳簿 (完成待ち) を一切通らず、桁あふれが起きようがありません。
  受理前に「実際の組み立て結果が Pattern の宣言どおりか」「1 回ぶん × 実行回数が
  ACO の集計と一致するか」を検証し、食い違えば受けません。
  取引のレシートは Quantum CPU に保存されるので、サーバーを再起動してもうしなわれません。
  ACO が無い・古い場合は連携ごと無効になり、従来の経路で動きます。
- **ポータブルセルに MEGA Cells の Greater Energy Card を挿せるようにしました** (×2)。
  MEGA Cells が自分のポータブルセルにしているのと同じ登録です。
- **Quantum CPU の完成品待ちを BigInteger で管理するようにしました** (PR #3、@syarukasu さん)。
  922京 (long の上限) を超える完成品もクランプせず正確に保持し、ME への搬入時だけ
  long の窓に切って渡します。保存形式も新しくなりますが、旧形式からは自動で移行します。
- **AE2 Crafting Optimizer (ACO) 1.5.12 以上との任意連携を追加しました** (PR #3 / #7 / #8、
  @syarukasu さん)。導入されていて BigInteger バックエンドが有効な場合、完成品待ち台帳と
  正確な (long を超える) クラフト計画を ACO の公開 API へ接続します。922京超の注文は
  提出時に正確な必要容量と CPU 容量を比較し、実行時は long に収まる窓へ分割して処理します。
  実行中の正確な残数はクラフト CPU に保存されるため、サーバーを再起動しても続きから
  再開できます。無くても内蔵の同じ台帳で動作は変わりません。保存形式は接続先によらず
  共通で、後から ACO を抜いてもデータは読めます (ACO 1.5.11 以前は連携が無効になり、
  内蔵台帳で動作します)。**ACO を入れるなら 1.5.18 以上を推奨します** —
  922京超の正確な計画を受け取れるのが 1.5.16 からで、それより前は連携が
  内蔵台帳の動作へ落ちます (1.5.13 には通常のクラフト計算まで失敗する不具合
  `StalePlanningSnapshotException` もあり、1.5.14 で修正されています)。
  1.5.17 以前は、**完成品 1 個あたりの下位パターン実行回数が 2^63 を超える注文**
  (8 倍圧縮 21 段など) が `WidePlanUnavailableException` で失敗しました。素材が
  足りないうちは「作成不可」で静かに終わり、素材を揃えた瞬間に落ちるようになる
  という症状です。1.5.18 で修正されました
  ([ACO #79](https://github.com/syarukasu/ae2-crafting-optimizer/issues/79))。
  この階層の計画は ACO 自身の実行経路では扱えず、Quantum CPU が引き受けます。
- **クラフト CPU の容量を BigInteger で正確に合算するようにしました** (同 PR)。
  8E クラフトストレージを正確な `2^63 B` として扱い、8E を 2 個接続した CPU は
  内部判定・GUI とも正しく `16E` になります。これまでの「合計が long を超えると
  CPU が壊れるため 8E は 1 CPU に 1 個まで」という制限が無くなりました。
  long を超えた容量は CPU 一覧に指数表記で表示されます。

### 修正

- **すべての名前・説明文が翻訳キーのまま表示される不具合を修正しました**。
  言語ファイル (`en_us.json` / `ja_jp.json`) の JSON が壊れており、
  Minecraft は壊れた言語ファイルを**エラーも出さずまるごと捨てる**ため、
  Mod の項目名が全部 `item.insaneae....` の生キー表示になっていました。
- **超強化クリエイティブセルを入れると、あらゆるクラフトが計算に失敗する不具合を
  修正しました**。セルが名乗る BigInteger 在庫が大きすぎ、ACO の計画エンジンの
  桁の天井 (`bigIntegerMaximumBits`、最大 54,427 bit) を越えていたため、
  計算が例外で終わって `WidePlanUnavailableException` になっていました
  (受け渡しの符号化の上限 1,048,576 bit と取り違えていたのが原因です)。
  名乗る量は ACO の設定値から実行時に決めるようにし、常に天井の半分に収まります。
  **BigInteger の計画が立たないと Quantum CPU も 922京/tick で頭打ちになる**ので、
  そちらの症状もこの修正で解消するはずです。
- **タスク統合カードが BigInteger 経路で効いていなかった不具合を修正しました**。
  ACO の正確な計画を受け取ったときだけ通る実行経路が統合の判定を見ておらず、
  クラフト CPU の予算で刻まれ続けていました。**922京級の注文ほどカードが効かない**という
  逆の症状になります (通常の注文では効いていたため気付きにくい状態でした)。
- **Advanced AE のクラフト CPU で、巨大な協調処理数だとクラフトが進まなくなる不具合を
  修正しました**。Advanced AE は AE2 のクラフト CPU を使わず自前の複製を持っており、
  1 tick の予算計算が int のままなので桁あふれします。AE2 側と同じ long 化を入れました。
  Advanced Quantum Engineering の BigInteger 量子コア (協調処理数の既定値が約 21 億) を
  使うと、**クラフトは始まるのに数 tick で止まる**という症状で表に出ます。
- **(1.20.1) 超特大インターフェイスの画面が開けない不具合を修正しました**。
  AE2 15.x では画面レイアウトの include が二重になり読み込みに失敗していました
  (例外はパケット処理に握り潰されるため、無反応・無ログで開けない症状になります)。
- **8^21 (= 2^63) 級の超巨大ジョブでクラフト計算の内部計算があふれる不具合を修正しました**
  (報告: @syarukasu さん)。材料の合計が long で表現できない要求は、負の数量が計画に
  紛れ込む代わりに「作成不可」として綺麗に失敗するようになります。
  8^20 までのジョブは従来どおり計算できます。
- **Advanced AE のクラフト CPU でまとめクラフトが効かなくなっていた不具合を修正しました**
  (Issue #2)。Advanced AE は進捗カウンタまで自前のコピーで持っており (1.3.6 / 1.6.12
  で確認)、型検査で弾かれて 1 クラフトずつの遅い経路に落ちていました。
  カウンタの型を問わない呼び方に変えています。
- **超高密度エナジーセルの日本語名を「極超高密度エナジーセル」に変更しました**
  (Issue #1)。MEGA Cells の Superdense Energy Cell の日本語名と同名になっていました。
- 超巨大ジョブのまとめ計算で、2 のべき乗の段数を数える内部計算が long の端で
  折り返す可能性を修正しました (PR #7 / #8、@syarukasu さん)。
- **AE2 Crafting Optimizer (ACO) と併用すると、特大パターンプロバイダーや Quantum CPU に
  入れたばかりのパターンが計算に使われないことがある不具合を修正しました。**
  パターン一覧の再読み込みを 1 tick ぶんまとめていたため、グリッドのクラフト索引の更新も
  1 tick 遅れていました (ACO は発注の時点で索引を写し取るので、その 1 tick が
  「不足アイテム」として表に出ます)。同じ tick の 2 回目以降だけをまとめるようにしたので、
  まとめて出し入れしたときの負荷は変わりません。

## 1.0.4

### 追加

- **超特大MEインターフェース** を追加しました。設定・在庫とも 9x9 = 81 枠で、
  1 枠に 21 億 4748 万個まで在庫できます。枠に入り切らない量は ME ネットワークへ
  直接入るので、Mekanism 系の機械が化学物質を long でまとめて送ってきても欠けません。
  壊したときは中身をまずネットワークへ戻します (AE2 のドロップ処理は
  1000 スタックを超えたぶんを黙って捨てるため)。
- **特大パターンプロバイダー** を追加しました。Quantum CPU と同じ
  1620 枠 (9x6 × 30 ページ) のパターンプロバイダで、加工パターンの置き場はこちらです。

### 変更

- **Quantum CPU は加工 (処理) パターンを受け付けなくなりました。** 自分で組める
  パターン専用になります。加工パターンは特大パターンプロバイダーへ。
  この変更より前に入れてあった加工パターンはそのまま取り出せますし、
  従来どおり隣接インベントリへの押し出しも動きます。

### 修正

- **追加した ME ストレージセルにアップグレードカードを挿せない不具合を修正しました。**
  あいまい/白黒リスト/均等配分/超過破棄カード (ポータブルはエネルギーカード ×2 も) が
  AE2 のセルと同じように使えます。
- **上位加速カードが ExtendedAE / Advanced AE の機械で使えない不具合を修正しました。**
  AE2 の加速カードが挿せる機械 (Ex 入出力バス各種・拡張分子組立装置・拡張刻印機・
  Ex IO ポート・回路切断機・アクティブ成形プレーン・Quantum Crafter・Reaction Chamber など、
  1.21 では水晶組立機も) に挿せて、倍率もきちんと効きます。
- (1.21.1) 一部のブロックがネットワークに繋がらない不具合を修正しました
  (NeoForge の capability 登録漏れ)。

## 1.0.3

### 修正

- **まとめクラフトでアイテムが増殖する不具合を修正しました。** クラフト CPU の在庫が
  要求回数ぶんに足りないとき、材料を上回る個数の完成品ができていました。中間素材が
  順次でき上がる多段クラフトでは通常の進行状態なので、日常的に踏む経路でした
  (回帰テストで確認したところ最大 51 倍)。
- 組み立てに失敗したとき、まとめ処理ぶんの材料が消える不具合を修正しました。
  レシピが差し替わった／消えた場合に起きます。

### 改善

- **Advanced AE のクラフト CPU でも Quantum CPU の高速化が効くようになりました。**
  Advanced AE は AE2 のクラフト CPU を使わず自前の複製を持っているため、
  これまで高速化が素通りされて 1 クラフトずつの遅い経路に落ちていました
  (報告のあった環境では 353ms/tick)。Advanced Quantum Engineering など
  Advanced AE の派生アドオンにも同時に効きます。
- 組み立て結果のキャッシュをパターンごとに持つようにしました。同時に走る小レシピが
  交互に来ると命中率が 0 に落ちていたのを解消しています。
- エネルギーセル 13 階層の色を組み直しました。上位 5 段がどれも同じピンクに見えていたのを、
  暖色まで回しきって 12 段すべてが一目で区別できるようにしています。
- 2G 協調処理ユニットと最上段エネルギーセルの虹色が濁っていたのを直しました。
- ポータブルセルの画面の色を AE2 / MEGA Cells / Applied Mekanistics と揃えました。

### 1.21.1 (NeoForge)

- **Minecraft 1.21.1 / NeoForge に対応しました。**
- セルのアイコンが透明になる不具合を修正しました (1.21 で色ハンドラの戻り値の
  アルファが描画に効くようになったため)。
- セルの色レイヤの形を MEGA Cells 4.11 に合わせました。
